# ============================================
# 环境变量配置模板
# 复制此文件并填入实际值
# 开发环境: cp .env.example .env.dev
# 生产环境: cp .env.example .env.prod
# ============================================

# --------- 服务器配置 ---------
SERVER_PORT=36600

# --------- 数据库配置 ---------
# [必填] 数据库密码
DB_PASSWORD=
# 开发环境使用 SSH 隧道时的本地端口
DB_LOCAL_PORT=15432
DB_USERNAME=postgres
# 生产环境直连配置
DB_HOST=localhost
DB_PORT=5432

# --------- Redis 配置 ---------
# [必填] Redis 密码
REDIS_PASSWORD=
# 开发环境使用 SSH 隧道时的本地端口
REDIS_LOCAL_PORT=16379
# 生产环境直连配置
REDIS_HOST=localhost
REDIS_PORT=6379

# --------- SSH 隧道配置（仅开发环境） ---------
# [必填-开发] SSH 服务器地址
SSH_HOST=
SSH_PORT=22
# [必填-开发] SSH 用户名和密码
SSH_USERNAME=
SSH_PASSWORD=

# --------- JWT 配置 ---------
# [必填] JWT 密钥，至少 64 字符
JWT_SECRET=
# Token 过期时间（秒），默认 6 小时
JWT_EXPIRATION=21600

# --------- CORS 配置（仅生产环境） ---------
# 允许的跨域来源，多个用逗号分隔
CORS_ALLOWED_ORIGINS=https://your-domain.com

# --------- 日志配置（仅生产环境） ---------
LOG_PATH=/var/log/movk-backend
